export class DirWatcher {
  
  constructor() {
    console.log('DirWatcher module'); // eslint-disable-line no-console
  }
  
  /**
   * Watch a given path with a given delay and
   * emit a ‘changed‘ event if directory contents have been changed
   *
   * When the path is checked for the first time all files treated as new
   *
   * @param path    directory path for watching
   * @param delay   delay in ms at which method works
   *
   * @return
   */
  
  watch(path, delay) {
  
  }
}
